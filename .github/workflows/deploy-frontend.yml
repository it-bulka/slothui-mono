name: Deploy Frontend

on:
  push:
    branches: [main]
    paths:
      - 'front/**'

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Frontend changes detected
        run: echo "Vercel will auto-deploy via its GitHub integration"
