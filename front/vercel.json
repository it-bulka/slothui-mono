{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "ignoreCommand": "git fetch --deepen=2 2>/dev/null; ! git diff HEAD~1 HEAD --name-only 2>/dev/null | grep -q '^front/'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
